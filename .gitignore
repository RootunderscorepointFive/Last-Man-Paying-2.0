# Dependencies (only needed locally for the migration script)
node_modules/

# Secrets — emails live here, loaded via Vercel secret. NEVER commit.
emails.json
.env
.env.local

# Lighthouse / audit / screenshot scratch
rendered_dom.html
screenshot_*.png
shot_*.png
shot.js
probe.js
terminal_dbg.html
nul

# OS
.DS_Store
Thumbs.db
